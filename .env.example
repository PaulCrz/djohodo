# Djohodo — environment template
# Copy to `.env` and fill in. Never commit your real `.env`.

# --- Model selection ---------------------------------------------------------
# Default model id used by watcher.agent. Haiku 4.5 is the cheapest current model.
# Switch to "claude-sonnet-4-6" later when richer analysis is needed.
DJOHODO_MODEL=claude-haiku-4-5-20251001

# --- Authentication ----------------------------------------------------------
# Pick ONE of the two paths:
#
# (a) Subscription credit (recommended for local use):
#     - Make sure ANTHROPIC_API_KEY is UNSET (comment it out below).
#     - Run `claude login` once so the Agent SDK uses your subscription credit.
#
# (b) Pay-as-you-go:
#     - Set ANTHROPIC_API_KEY below.
#
# ANTHROPIC_API_KEY=sk-ant-...

# --- Delivery (SMTP, optional) ----------------------------------------------
# Set to 1 to also email the digest. All SMTP_* vars are then required.
DJOHODO_EMAIL_ENABLED=0
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASSWORD=change-me
SMTP_FROM=you@example.com
SMTP_TO=you@example.com

# --- Delivery (WhatsApp via Meta Cloud API, optional) -----------------------
# Set to 1 to also send the digest to WhatsApp.
DJOHODO_WHATSAPP_ENABLED=0
# From Meta Business / WhatsApp Manager.
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
# E.164 without the leading '+' (e.g. 33612345678).
WHATSAPP_RECIPIENT=
# 'text' — free-form text; only works inside the 24h customer-service window
#          opened by the recipient. Good for interactive testing.
# 'template' — sends a Meta-approved template. Required for unattended cron.
#              The template must have one {{1}} body parameter.
WHATSAPP_MODE=template
WHATSAPP_TEMPLATE_NAME=
WHATSAPP_TEMPLATE_LANGUAGE=fr

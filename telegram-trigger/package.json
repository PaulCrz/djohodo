{
  "name": "djohodo-trigger",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that receives Telegram /watch commands and dispatches the Djohodo daily-watch workflow on GitHub Actions.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.97.0"
  }
}

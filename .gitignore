# Environments & secrets
.env
.env.*
!.env.example

# Generated digests (the .gitkeep is checked in)
digests/*
!digests/.gitkeep

# Resolver ticker → canonical name cache (restored across runs via
# actions/cache in CI; lives locally on dev machines).
cache/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
.DS_Store
